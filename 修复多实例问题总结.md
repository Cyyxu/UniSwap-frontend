# 修复多 Axios 实例导致的 Token 刷新不同步问题

## 🎯 问题根源

你的应用中存在**两套 Axios 实例**：

1. **主实例** (`src/api/request.ts`)
   - ✅ 有完整的 Token 刷新机制
   - ✅ 有刷新锁和请求队列
   - ✅ 从内存读取 Token
   - ✅ 自动重试失败的请求

2. **AI 实例** (`src/api/ai.ts` 的 `aiApiInstance`)
   - ❌ 没有 Token 刷新机制
   - ❌ 从 localStorage 读取 Token（已过时）
   - ❌ 遇到 401 直接跳转登录
   - ❌ 不会自动重试

## 🐛 问题表现

当页面同时发起多个请求时（如主业务 + AI 消息加载）：

```
时间线：
T0: 用户打开页面
T1: 同时发起 /api/score/mine 和 /api/llm/history
T2: 两个请求都遇到 401 (Token 过期)

主实例的处理：
✅ request.ts 拦截到 401
✅ 开始刷新 Token (isRefreshing = true)
✅ 刷新成功，更新内存中的 Token
✅ 自动重试 /api/score/mine
✅ 请求成功，数据正常显示

AI 实例的处理：
❌ aiApiInstance 拦截到 401
❌ 直接抛出错误："用户未登录，请先登录"
❌ 页面显示错误提示
❌ 用户体验中断
```

## ✅ 解决方案

**统一使用 `request.ts` 实例**，删除 `aiApiInstance`。

### 修改前（ai.ts）

```typescript
// ❌ 独立的 Axios 实例
const aiApiInstance = axios.create({
  baseURL: aiBaseURL,
  timeout: 120000,
  withCredentials: true,
})

// ❌ 自己的拦截器（没有刷新机制）
aiApiInstance.interceptors.request.use(...)
aiApiInstance.interceptors.response.use(...)

export const aiApi = {
  add: (data) => aiApiInstance.post('/api/llm/chat', data),
  getMyList: (params) => aiApiInstance.post('/api/llm/history', params),
  delete: (id) => aiApiInstance.post('/api/llm/remove', { id }),
}
```

### 修改后（ai.ts）

```typescript
// ✅ 复用主请求实例
import api from './request'

export const aiApi = {
  // ✅ 使用主 api 实例，但为 AI 请求设置更长的超时时间
  add: (data: AIMessageAddRequest) => 
    api.post<AIMessage>('/api/llm/chat', data, { 
      timeout: 120000 // AI 请求需要 120 秒超时
    }),
  
  getMyList: (params: AIMessageQuery) => 
    api.post('/api/llm/history', params, { 
      timeout: 120000 
    }),
  
  delete: (id: number) => 
    api.post<boolean>('/api/llm/remove', { id }, { 
      timeout: 120000 
    }),
}
```

## 🎉 修复效果

现在当 AI 请求遇到 401 时：

```
时间线：
T0: 用户打开页面
T1: 同时发起 /api/score/mine 和 /api/llm/history
T2: 两个请求都遇到 401 (Token 过期)

统一处理：
✅ request.ts 拦截到第一个 401
✅ 设置 isRefreshing = true
✅ 第二个 401 进入等待队列
✅ 刷新 Token 成功
✅ 自动重试 /api/score/mine
✅ 自动重试 /api/llm/history
✅ 两个请求都成功
✅ 用户完全无感知
```

## 📊 代码对比

### 代码量

- **修改前**: 100+ 行（独立实例 + 拦截器）
- **修改后**: 30 行（只保留接口定义）
- **减少**: 70% 的代码

### 维护性

- **修改前**: 需要在两个地方维护相同的逻辑
- **修改后**: 只需要维护 `request.ts` 一个地方

### 可靠性

- **修改前**: AI 请求会导致登录失效
- **修改后**: 所有请求都享受无感刷新

## 🔍 验证方法

1. **登录系统**

2. **打开浏览器 F12 -> Network 面板**

3. **等待 Token 过期（或手动删除）**

4. **同时触发多个请求**
   - 点击"我的积分"（主业务）
   - 点击"AI 聊天"（AI 业务）

5. **观察 Network 面板**

应该看到：
```
/api/score/mine     -> 🔴 401 (第一个失败)
/api/llm/history    -> 🔴 401 (第二个失败)
/api/user/refresh   -> 🟢 200 (自动刷新)
/api/score/mine     -> 🟢 200 (自动重试成功)
/api/llm/history    -> 🟢 200 (自动重试成功)
```

6. **观察页面**
   - ✅ 没有错误提示
   - ✅ 没有跳转到登录页
   - ✅ 数据正常显示
   - ✅ 用户完全无感知

## 💡 最佳实践

### 1. 单一 Axios 实例原则

**一个应用只应该有一个 Axios 实例**，所有请求都通过它发送。

```typescript
// ✅ 好的做法
import api from './request'

export const userApi = {
  getInfo: () => api.get('/api/user/info'),
  update: (data) => api.put('/api/user/update', data),
}

export const aiApi = {
  chat: (data) => api.post('/api/llm/chat', data, { timeout: 120000 }),
}
```

```typescript
// ❌ 不好的做法
const userApiInstance = axios.create({ ... })
const aiApiInstance = axios.create({ ... })
const orderApiInstance = axios.create({ ... })
// 每个实例都要配置拦截器，维护成本高，容易出错
```

### 2. 特殊配置通过参数传递

如果某个请求需要特殊配置（如超时时间），通过第三个参数传递：

```typescript
// ✅ 正确：通过参数覆盖默认配置
api.post('/api/llm/chat', data, { 
  timeout: 120000,  // 覆盖默认的 30 秒
  headers: { 'X-Custom': 'value' }
})
```

```typescript
// ❌ 错误：为了改超时时间就创建新实例
const longTimeoutApi = axios.create({ timeout: 120000 })
```

### 3. 不同 BaseURL 的处理

如果需要请求不同的服务器：

```typescript
// ✅ 方法 1：通过参数覆盖 baseURL
api.get('/messages', { 
  baseURL: 'https://api.ai-service.com' 
})

// ✅ 方法 2：使用完整 URL
api.get('https://api.ai-service.com/messages')

// ❌ 错误：创建新实例
const aiServiceApi = axios.create({ 
  baseURL: 'https://api.ai-service.com' 
})
```

### 4. Token 存储统一

```typescript
// ✅ 正确：统一从内存读取
import { getAccessToken } from '@/utils/auth'
const token = getAccessToken()

// ❌ 错误：混用多种存储方式
const token1 = localStorage.getItem('token')  // ❌
const token2 = getAccessToken()               // ✅
const token3 = sessionStorage.getItem('token') // ❌
```

## 🚀 后续优化建议

1. **检查其他模块**
   - 确保没有其他地方创建了独立的 Axios 实例
   - 统一使用 `request.ts`

2. **添加请求日志**
   - 在 `request.ts` 中添加统一的请求日志
   - 方便排查问题

3. **错误处理统一**
   - 所有错误都在 `request.ts` 中统一处理
   - 业务代码只需要关注成功的情况

4. **类型安全**
   - 为所有 API 接口添加 TypeScript 类型
   - 提高代码质量

## 📝 检查清单

- [x] 删除 `ai.ts` 中的 `aiApiInstance`
- [x] 改用 `request.ts` 的 `api` 实例
- [x] 为 AI 请求设置 120 秒超时
- [x] 验证代码没有语法错误
- [x] 确认其他 API 文件都使用 `request.ts`
- [ ] 测试 Token 过期时的自动刷新
- [ ] 测试并发请求的处理
- [ ] 测试 AI 请求的超时处理

## 🎯 总结

通过统一使用 `request.ts` 实例，你的应用现在：

1. ✅ **所有请求**都享受无感刷新机制
2. ✅ **并发请求**不会导致多次刷新
3. ✅ **代码更简洁**，减少 70% 的重复代码
4. ✅ **维护更容易**，只需要维护一个地方
5. ✅ **用户体验更好**，不会因为 AI 请求失败而中断

**现在去测试吧！** 🚀

同时触发主业务和 AI 请求，观察它们是否都能自动刷新并重试成功。
