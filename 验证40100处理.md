# 验证 40100 错误码处理

## 问题分析

你看到的错误：
```json
{
  "errorCode": 40100,
  "errorMsg": "用户未登录，请先登录",
  "timestamp": "2026-02-02 22:51:23"
}
```

这是后端返回的 Token 过期错误。前端应该：
1. 检测到 `errorCode: 40100`
2. 自动调用刷新接口
3. 刷新成功后重试原始请求
4. 用户完全无感知

## 代码逻辑

### 响应拦截器（request.ts）

```typescript
api.interceptors.response.use(
  (response) => {
    const res = response.data
    
    // 🔥 第一步：检测 40100（最高优先级）
    if (res.code === 40100 || res.code === '40100' || res.errorCode === 40100) {
      console.log('[Token] 检测到 Token 过期（code: 40100），触发刷新')
      return handleTokenExpired(response.config)
    }
    
    // 其他逻辑...
  }
)
```

### 关键点

1. **40100 检测必须在最前面**
   - 避免被其他错误处理逻辑拦截
   - 直接返回 `handleTokenExpired` 的 Promise

2. **支持多种字段名**
   - `res.code === 40100`
   - `res.errorCode === 40100`
   - 字符串和数字都支持

## 测试步骤

### 1. 打开浏览器控制台

按 F12，切换到 Console 标签页

### 2. 清空控制台

点击 🚫 图标清空之前的日志

### 3. 触发一个请求

点击页面上的任意按钮（如"我的积分"、"AI 聊天"等）

### 4. 观察控制台日志

**✅ 正确的日志顺序**：
```
[API Request] GET /api/score/mine
[API Request] 已添加 Token: eyJhbGciOiJIUzM4NCJ9...
[Token] 检测到 Token 过期（code: 40100），触发刷新
[Token Refresh] 开始刷新 Token...
[Token Refresh] 刷新接口响应: { errorCode: 0, data: { accessToken: 'xxx' } }
[Token Refresh] ✅ 刷新成功，新 Token: eyJhbGciOiJIUzM4NCJ9...
[Token Refresh] 执行队列中的 0 个请求
[Token Refresh] 🔄 重试原始请求: /api/score/mine
[API Request] GET /api/score/mine
[API Request] 已添加 Token: eyJhbGciOiJIUzM4NCJ9...
```

**❌ 错误的表现**：
```
[API Request] GET /api/score/mine
[API Request] 已添加 Token: eyJhbGciOiJIUzM4NCJ9...
❌ 用户未登录，请先登录 (antd message)
❌ 没有看到 [Token] 检测到 Token 过期
❌ 没有看到刷新流程
```

### 5. 观察 Network 面板

切换到 Network 标签页，应该看到：

```
/api/score/mine     -> 200 OK (但返回 errorCode: 40100)
/api/user/refresh   -> 200 OK (刷新成功)
/api/score/mine     -> 200 OK (重试成功，返回真实数据)
```

### 6. 观察页面

- ✅ 没有错误提示
- ✅ 没有跳转到登录页
- ✅ 数据正常显示

## 可能的问题

### 问题 1：看不到 "[Token] 检测到 Token 过期" 日志

**原因**：40100 检测逻辑没有生效

**排查**：
1. 检查后端返回的字段名是 `code` 还是 `errorCode`
2. 检查值是数字 `40100` 还是字符串 `"40100"`

**解决**：代码已经支持所有情况
```typescript
if (res.code === 40100 || res.code === '40100' || res.errorCode === 40100)
```

### 问题 2：看到了检测日志，但没有刷新

**原因**：`handleTokenExpired` 函数执行失败

**排查**：
1. 查看控制台是否有 "[Token Refresh] 开始刷新 Token..." 日志
2. 查看是否有刷新失败的错误日志

**解决**：检查刷新接口是否正常

### 问题 3：刷新成功，但没有重试原始请求

**原因**：缺少 `return api(originalRequest)`

**排查**：
1. 查看是否有 "[Token Refresh] 🔄 重试原始请求" 日志
2. 查看 Network 面板是否有第二次请求

**解决**：代码已经包含重试逻辑

### 问题 4：显示错误提示 "用户未登录，请先登录"

**原因**：40100 被其他错误处理逻辑拦截了

**排查**：
1. 检查 40100 检测是否在最前面
2. 检查是否有其他地方也在处理错误

**解决**：确保 40100 检测在响应拦截器的第一行

## 调试技巧

### 1. 添加更多日志

在 `request.ts` 的响应拦截器开头添加：

```typescript
api.interceptors.response.use(
  (response) => {
    const res = response.data
    
    // 🔍 调试日志
    console.log('[Response] URL:', response.config.url)
    console.log('[Response] Data:', res)
    console.log('[Response] errorCode:', res.errorCode, 'code:', res.code)
    
    // 40100 检测...
  }
)
```

### 2. 使用断点

在浏览器 DevTools 中：
1. 切换到 Sources 标签页
2. 找到 `request.ts` 文件
3. 在第 83 行（40100 检测）打断点
4. 触发请求，观察变量值

### 3. 检查后端返回

在 Network 面板中：
1. 找到返回 40100 的请求
2. 点击查看 Response
3. 确认字段名和值的类型

## 预期结果

当一切正常时，你应该看到：

1. **控制台**：完整的刷新流程日志
2. **Network**：3 个请求（失败 → 刷新 → 重试成功）
3. **页面**：数据正常显示，无错误提示
4. **用户体验**：完全无感知

## 如果还是不行

请提供以下信息：

1. **完整的控制台日志**（从请求开始到结束）
2. **Network 面板截图**（显示所有请求）
3. **后端返回的完整 Response**（包括 Headers 和 Body）

这样我可以更准确地定位问题。

---

**现在去测试吧！** 🚀

刷新页面，点击任意按钮，观察控制台是否有 "[Token] 检测到 Token 过期" 日志。
