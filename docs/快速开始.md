# Token 安全改造 - 快速开始

## 🎯 改造目标

将前端认证从 `localStorage` 存储改为**内存变量 + HttpOnly Cookie**，实现静默刷新机制，防止 XSS 攻击窃取 Token。

---

## ✅ 前端改造（已完成）

前端所有改造已完成，包括：
- ✅ Token 内存管理模块
- ✅ Axios 静默刷新拦截器
- ✅ 应用初始化自动恢复登录
- ✅ 路由守卫更新
- ✅ 完整文档

**无需额外操作，可以直接进行后端开发！**

---

## 🔧 后端改造（待完成）

### 第一步：添加依赖（如果还没有）

```xml
<!-- pom.xml -->
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt</artifactId>
    <version>0.9.1</version>
</dependency>
```

### 第二步：配置文件

```yaml
# application.yml
jwt:
  secret: your-secret-key-at-least-256-bits-long-for-security
  access-token-expiration: 1800000   # 30分钟
  refresh-token-expiration: 604800000 # 7天
```

### 第三步：创建刷新接口

在 `UserController` 中添加：

```java
@PostMapping("/refresh")
public Result<Map<String, String>> refresh(HttpServletRequest request) {
    // 1. 从 Cookie 获取 Refresh Token
    String refreshToken = null;
    Cookie[] cookies = request.getCookies();
    if (cookies != null) {
        for (Cookie cookie : cookies) {
            if ("refreshToken".equals(cookie.getName())) {
                refreshToken = cookie.getValue();
                break;
            }
        }
    }
    
    // 2. 验证 Refresh Token
    if (refreshToken == null || !jwtUtil.validateRefreshToken(refreshToken)) {
        throw new BusinessException(ErrorCode.UNAUTHORIZED, "登录已过期");
    }
    
    // 3. 生成新的 Access Token
    Long userId = jwtUtil.getUserIdFromToken(refreshToken);
    String newAccessToken = jwtUtil.generateAccessToken(userId);
    
    // 4. 返回新 Token
    Map<String, String> result = new HashMap<>();
    result.put("accessToken", newAccessToken);
    return Result.success(result);
}
```

### 第四步：改造登录接口

在登录成功后添加 Cookie 设置：

```java
@PostMapping("/login")
public Result<LoginResponse> login(@RequestBody LoginRequest request, 
                                   HttpServletResponse response) {
    // ... 原有的登录逻辑 ...
    
    // 生成 Refresh Token
    String refreshToken = jwtUtil.generateRefreshToken(user.getId());
    
    // 设置到 HttpOnly Cookie
    Cookie cookie = new Cookie("refreshToken", refreshToken);
    cookie.setHttpOnly(true);  // 关键：防止 JS 读取
    cookie.setSecure(true);    // 生产环境：仅 HTTPS
    cookie.setPath("/");
    cookie.setMaxAge(7 * 24 * 60 * 60); // 7天
    response.addCookie(cookie);
    
    // 返回 Access Token
    return Result.success(loginResponse);
}
```

### 第五步：改造登出接口

清除 Cookie：

```java
@PostMapping("/logout")
public Result<Boolean> logout(HttpServletResponse response) {
    // 清除 Refresh Token Cookie
    Cookie cookie = new Cookie("refreshToken", null);
    cookie.setMaxAge(0);
    cookie.setPath("/");
    cookie.setHttpOnly(true);
    response.addCookie(cookie);
    
    return Result.success(true);
}
```

### 第六步：配置 CORS

```java
@Configuration
public class CorsConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
                .allowedOrigins("http://localhost:5173") // 前端地址
                .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
                .allowedHeaders("*")
                .allowCredentials(true) // 关键：允许 Cookie
                .maxAge(3600);
    }
}
```

---

## 🧪 测试验证

### 1. 启动项目

```bash
# 启动后端（确保在 8109 端口）
# 启动前端
npm run dev
```

### 2. 测试登录

1. 访问 http://localhost:5173/login
2. 输入账号密码登录
3. 打开开发者工具（F12）
4. 检查：
   - ✅ Application > Cookies 中有 `refreshToken`
   - ✅ Application > Local Storage 中**没有** `token`
   - ✅ 控制台输出：`[App Init] 静默刷新成功`

### 3. 测试刷新页面

1. 登录后按 F5 刷新页面
2. 检查：
   - ✅ 仍然保持登录状态
   - ✅ Network 中有 `/api/user/refresh` 请求
   - ✅ 控制台输出：`[Token Refresh] 刷新成功`

### 4. 测试自动刷新

1. 等待 Access Token 过期（或修改过期时间为 1 分钟）
2. 访问需要认证的页面
3. 检查：
   - ✅ 自动调用刷新接口
   - ✅ 页面正常加载，用户无感知

---

## 📚 详细文档

- **改造说明**：`docs/Token安全改造说明.md`
- **后端实现**：`docs/后端刷新接口实现示例.md`
- **测试指南**：`docs/测试验证指南.md`
- **检查清单**：`docs/迁移检查清单.md`

---

## ❓ 常见问题

### Q1: 刷新页面后跳转登录？
**A**: 检查后端是否实现了 `/api/user/refresh` 接口，以及 CORS 是否配置了 `allowCredentials(true)`。

### Q2: Cookie 无法设置？
**A**: 检查：
1. 后端 Cookie 的 `Path` 是否设置为 `/`
2. CORS 是否允许 credentials
3. 前端 Axios 是否配置了 `withCredentials: true`

### Q3: Token 刷新死循环？
**A**: 检查刷新接口路径判断是否正确：
```typescript
if (originalRequest.url?.includes('/user/refresh'))
```

### Q4: 开发环境 Cookie Secure 报错？
**A**: 开发环境可以设置 `cookie.setSecure(false)`，生产环境必须设置为 `true`。

---

## 🚀 部署到生产

### 前端
1. 修改 `.env.production`：
   ```
   VITE_API_BASE_URL=https://your-api-domain.com/uniswap
   ```
2. 构建：`npm run build`
3. 部署 `dist` 目录

### 后端
1. 确保 Cookie 设置了 `Secure = true`
2. 配置 HTTPS 证书
3. 更新 CORS 允许的域名

---

## 🎉 完成！

改造完成后，你的应用将拥有：
- ✅ 防 XSS 攻击的安全认证
- ✅ 自动刷新的无感体验
- ✅ 刷新页面保持登录
- ✅ 业界最佳实践的认证方案

**有问题？查看详细文档或联系团队！**
