# Token 安全改造迁移检查清单

## 前端改造清单

### ✅ 已完成的改造

#### 1. 核心文件创建
- [x] `src/utils/auth.ts` - Token 内存管理模块
  - `getAccessToken()` - 获取内存中的 Token
  - `setAccessToken(token)` - 设置 Token 到内存
  - `clearAccessToken()` - 清除内存中的 Token
  - `hasAccessToken()` - 检查是否有 Token

#### 2. 核心文件改造
- [x] `src/api/request.ts` - Axios 拦截器
  - 请求拦截器：从内存获取 Token
  - 响应拦截器：实现静默刷新和请求队列
  - 401 自动刷新机制
  - 刷新锁防止并发
  - 请求队列暂存机制

- [x] `src/store/authStore.ts` - 认证状态管理
  - `setToken()` 改为使用内存存储
  - `logout()` 改为清除内存 Token
  - 新增 `isAuthenticated()` 方法

- [x] `src/main.tsx` - 应用初始化
  - 添加 `initApp()` 函数
  - 应用启动时自动调用刷新接口
  - 恢复内存中的 Token

- [x] `src/App.tsx` - 路由守卫
  - `PrivateRoute` 改为使用 `hasAccessToken()`

- [x] `src/components/AdminRoute/index.tsx` - 管理员路由守卫
  - 改为使用 `hasAccessToken()` 判断

#### 3. 文档创建
- [x] `docs/Token安全改造说明.md` - 改造详细说明
- [x] `docs/后端刷新接口实现示例.md` - 后端实现参考
- [x] `docs/测试验证指南.md` - 测试步骤和方法
- [x] `docs/迁移检查清单.md` - 本文档

---

## 后端改造清单

### 🔲 待完成的后端改造

#### 1. 登录接口改造 (`/api/user/login`)
- [ ] 生成 Access Token（短期，15-30分钟）
- [ ] 生成 Refresh Token（长期，7-30天）
- [ ] 设置 Refresh Token 到 HttpOnly Cookie
- [ ] Cookie 属性设置：
  - [ ] `HttpOnly = true`
  - [ ] `Secure = true`（生产环境）
  - [ ] `SameSite = Strict`
  - [ ] `Path = /`
  - [ ] `MaxAge = 7天`
- [ ] 返回 Access Token 和用户信息

#### 2. 刷新接口实现 (`/api/user/refresh`)
- [ ] 创建刷新接口
- [ ] 从 Cookie 中读取 Refresh Token
- [ ] 验证 Refresh Token 有效性
- [ ] 生成新的 Access Token
- [ ] 返回新的 Access Token

#### 3. 登出接口改造 (`/api/user/logout`)
- [ ] 清除 Refresh Token Cookie
- [ ] 设置 `MaxAge = 0`

#### 4. JWT 工具类
- [ ] `generateAccessToken(userId)` - 生成 Access Token
- [ ] `generateRefreshToken(userId)` - 生成 Refresh Token
- [ ] `validateAccessToken(token)` - 验证 Access Token
- [ ] `validateRefreshToken(token)` - 验证 Refresh Token
- [ ] `getUserIdFromToken(token)` - 从 Token 提取用户 ID

#### 5. CORS 配置
- [ ] 允许前端域名
- [ ] 设置 `allowCredentials = true`
- [ ] 允许必要的 HTTP 方法和头

#### 6. 配置文件
- [ ] 配置 JWT Secret
- [ ] 配置 Access Token 过期时间
- [ ] 配置 Refresh Token 过期时间

---

## 环境配置清单

### 开发环境
- [ ] 前端代理配置（`vite.config.ts`）
  ```typescript
  server: {
    proxy: {
      '/uniswap': {
        target: 'http://localhost:8109',
        changeOrigin: true,
      }
    }
  }
  ```

- [ ] 后端 CORS 配置
  ```java
  .allowedOrigins("http://localhost:5173")
  .allowCredentials(true)
  ```

### 生产环境
- [ ] 前端环境变量（`.env.production`）
  ```
  VITE_API_BASE_URL=https://your-api-domain.com/uniswap
  ```

- [ ] 后端 Cookie Secure 设置
  ```java
  cookie.setSecure(true); // 仅 HTTPS
  ```

- [ ] HTTPS 证书配置
- [ ] CORS 配置生产域名

---

## 测试清单

### 功能测试
- [ ] 登录成功
- [ ] 刷新页面保持登录
- [ ] Token 自动刷新
- [ ] 并发请求只刷新一次
- [ ] Refresh Token 过期跳转登录
- [ ] 登出功能
- [ ] 管理员权限验证

### 安全测试
- [ ] localStorage 中无 Token
- [ ] Cookie 有 HttpOnly 标记
- [ ] XSS 攻击无法窃取 Token
- [ ] 网络抓包验证 Cookie 安全属性

### 性能测试
- [ ] 刷新请求响应时间 < 200ms
- [ ] 页面加载无明显延迟
- [ ] 并发请求优化生效

### 边界测试
- [ ] 网络异常处理
- [ ] 后端服务停止处理
- [ ] Token 格式错误处理

### 兼容性测试
- [ ] Chrome 浏览器
- [ ] Firefox 浏览器
- [ ] Edge 浏览器
- [ ] Safari 浏览器
- [ ] 移动端 iOS Safari
- [ ] 移动端 Android Chrome

---

## 代码审查清单

### 前端代码审查
- [x] 所有 `localStorage.getItem('token')` 已改为 `getAccessToken()`
- [x] 所有 `localStorage.setItem('token', ...)` 已改为 `setAccessToken(...)`
- [x] 所有 `localStorage.removeItem('token')` 已改为 `clearAccessToken()`
- [x] Axios 配置了 `withCredentials: true`
- [x] 响应拦截器正确处理 401 错误
- [x] 刷新接口路径判断正确
- [x] 请求队列机制实现正确

### 后端代码审查
- [ ] Cookie 设置了 HttpOnly
- [ ] Cookie 设置了 Secure（生产环境）
- [ ] Cookie 设置了 SameSite
- [ ] CORS 允许 credentials
- [ ] JWT Secret 足够复杂（至少 256 位）
- [ ] Token 过期时间合理
- [ ] 刷新接口验证 Refresh Token
- [ ] 登出接口清除 Cookie

---

## 部署清单

### 前端部署
- [ ] 构建生产版本
  ```bash
  npm run build
  ```
- [ ] 检查构建产物
- [ ] 上传到服务器
- [ ] 配置 Nginx/Apache
- [ ] 验证 HTTPS 证书
- [ ] 测试生产环境

### 后端部署
- [ ] 更新后端代码
- [ ] 配置生产环境变量
- [ ] 重启后端服务
- [ ] 验证刷新接口可用
- [ ] 检查日志无错误

---

## 回滚计划

### 如果出现问题，如何回滚？

#### 前端回滚
1. 恢复 `src/api/request.ts` 的旧版本
2. 恢复 `src/store/authStore.ts` 的旧版本
3. 删除 `src/utils/auth.ts`
4. 恢复 `src/main.tsx` 的旧版本
5. 重新构建和部署

#### 后端回滚
1. 恢复登录接口的旧版本
2. 删除刷新接口
3. 重启后端服务

#### 数据库回滚
- 本次改造不涉及数据库变更，无需回滚

---

## 监控和日志

### 前端监控
- [ ] 添加刷新成功/失败的埋点
- [ ] 监控刷新接口的调用频率
- [ ] 监控 401 错误的发生率

### 后端监控
- [ ] 监控刷新接口的调用量
- [ ] 监控刷新接口的成功率
- [ ] 监控 Token 验证失败的次数
- [ ] 记录异常刷新行为（可能的攻击）

### 日志记录
```typescript
// 前端日志
console.log('[Token Refresh] 开始刷新 Token...')
console.log('[Token Refresh] 刷新成功')
console.error('[Token Refresh] 刷新失败:', error)

// 后端日志
log.info("用户 {} 刷新 Token 成功", userId);
log.warn("用户 {} 刷新 Token 失败: {}", userId, error);
```

---

## 优化建议

### 短期优化（可选）
- [ ] 添加 Token 即将过期的提前刷新（提前 1-2 分钟）
- [ ] 添加刷新失败的重试机制（最多 3 次）
- [ ] 添加刷新中的 Loading 状态

### 长期优化（可选）
- [ ] 使用 Redis 存储 Refresh Token（实现强制登出）
- [ ] 实现 Token Rotation（每次刷新生成新的 Refresh Token）
- [ ] 实现单设备登录（覆盖旧 Token）
- [ ] 添加 IP 和 User-Agent 绑定
- [ ] 实现"记住我"功能（调整过期时间）

---

## 文档更新

- [x] 创建改造说明文档
- [x] 创建后端实现示例
- [x] 创建测试验证指南
- [x] 创建迁移检查清单
- [ ] 更新项目 README
- [ ] 更新 API 文档
- [ ] 更新团队开发规范

---

## 团队沟通

### 需要通知的人员
- [ ] 前端开发团队
- [ ] 后端开发团队
- [ ] 测试团队
- [ ] 运维团队
- [ ] 产品经理

### 需要培训的内容
- [ ] 新的认证机制原理
- [ ] 如何测试和验证
- [ ] 如何排查问题
- [ ] 如何监控和维护

---

## 时间规划

### 建议的实施步骤

#### 第 1 天：后端开发
- 上午：实现刷新接口
- 下午：改造登录和登出接口
- 晚上：本地测试

#### 第 2 天：联调和测试
- 上午：前后端联调
- 下午：功能测试和安全测试
- 晚上：修复问题

#### 第 3 天：部署和验证
- 上午：部署到测试环境
- 下午：全面测试
- 晚上：部署到生产环境

#### 第 4 天：监控和优化
- 全天：监控生产环境
- 及时处理问题
- 收集反馈

---

## 成功标准

### 功能标准
- ✅ 所有功能测试通过
- ✅ 所有安全测试通过
- ✅ 所有边界测试通过
- ✅ 所有兼容性测试通过

### 性能标准
- ✅ 刷新请求响应时间 < 200ms
- ✅ 页面加载时间无明显增加
- ✅ 并发请求优化生效

### 安全标准
- ✅ localStorage 中无敏感信息
- ✅ Cookie 有正确的安全属性
- ✅ XSS 攻击无法窃取 Token
- ✅ CSRF 防护生效

### 用户体验标准
- ✅ 用户无感知
- ✅ 刷新页面保持登录
- ✅ 无需频繁登录
- ✅ 无明显卡顿

---

## 总结

### 前端改造已完成 ✅
- 所有核心文件已改造完成
- 所有文档已创建完成
- 代码无语法错误
- 可以开始后端开发和联调

### 后端改造待完成 🔲
- 参考 `docs/后端刷新接口实现示例.md`
- 实现三个核心接口
- 配置 CORS 和 Cookie

### 下一步行动
1. 后端开发实现刷新接口
2. 前后端联调测试
3. 按照测试指南进行全面测试
4. 部署到测试环境
5. 部署到生产环境
6. 持续监控和优化

---

**祝改造顺利！🎉**
