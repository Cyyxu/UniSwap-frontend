# 测试无感刷新功能

## ✅ 已完成的修复

### 核心改进点

1. **统一处理 Token 过期**
   - 支持 HTTP 401 状态码
   - 支持后端自定义 `code: 40100` 错误码
   - 两种情况都会触发自动刷新

2. **完整的自动重试机制**
   - 拦截失败的请求
   - 自动调用刷新接口换新 Token
   - **自动重试原始请求**（这是关键！）
   - 用户完全无感知

3. **并发请求处理**
   - 使用 `isRefreshing` 锁防止重复刷新
   - 使用 `requestsQueue` 队列暂存等待的请求
   - 刷新成功后批量重试所有挂起的请求

4. **兼容多种返回格式**
   - `{ errorCode: 0, data: { accessToken: 'xxx' } }`
   - `{ code: 200, data: { accessToken: 'xxx' } }`
   - `{ accessToken: 'xxx' }`

## 🧪 测试步骤

### 方法一：修改后端 Token 过期时间（推荐）

1. **修改后端配置**
   ```yaml
   # application.yml
   jwt:
     access-token-expiration: 10000  # 改为 10 秒
   ```

2. **重启后端服务**

3. **测试流程**
   ```
   1. 登录系统
   2. 打开浏览器 F12 -> Network 面板
   3. 等待 15 秒（让 Token 过期）
   4. 点击任意查询按钮（如"获取用户信息"）
   ```

4. **观察 Network 面板**（成功标志）
   ```
   第 1 行：/api/user/info     -> 🔴 401 (旧 Token 失效)
   第 2 行：/api/user/refresh  -> 🟢 200 (自动换票)
   第 3 行：/api/user/info     -> 🟢 200 (自动重试成功)
   ```

5. **观察页面**（成功标志）
   - ✅ 页面没有报错
   - ✅ 页面没有跳转到登录页
   - ✅ 数据正常显示
   - ✅ 用户完全不知道发生了什么

### 方法二：手动删除 Token（快速测试）

1. **登录系统**

2. **打开浏览器控制台（Console）**
   ```javascript
   // 清空内存中的 Token（模拟过期）
   // 注意：你的 Token 存在内存中，所以需要通过 API 调用来触发
   ```

3. **直接等待 Token 自然过期**
   - 如果后端 Token 时间较长，可以先登录
   - 然后关闭浏览器
   - 第二天再打开（Token 肯定过期了）
   - 点击任意按钮，观察是否自动刷新

### 方法三：使用 Chrome DevTools 模拟慢网络

1. **登录系统**

2. **F12 -> Network -> Throttling -> Slow 3G**

3. **等待 Token 过期后点击按钮**

4. **观察请求顺序**（应该能清楚看到 3 个请求）

## 🔍 调试日志

打开浏览器控制台（Console），你会看到详细的日志：

```
[Token] 检测到 Token 过期（code: 40100），触发刷新
[Token Refresh] 开始刷新 Token...
[Token Refresh] 刷新接口响应: { errorCode: 0, data: { accessToken: 'xxx' } }
[Token Refresh] ✅ 刷新成功，新 Token: eyJhbGciOiJIUzUxMiJ9...
[Token Refresh] 执行队列中的 2 个请求
[Token Refresh] 🔄 重试原始请求: /api/user/info
[API Request] GET /api/user/info
[API Request] 已添加 Token: eyJhbGciOiJIUzUxMiJ9...
```

## 🎯 预期结果

### ✅ 成功的表现

1. **用户视角**
   - 点击按钮 → 数据正常显示
   - 没有任何错误提示
   - 没有跳转到登录页
   - 体验流畅，完全无感知

2. **开发者视角（Network 面板）**
   - 第一个请求报 401
   - 自动调用 refresh 接口
   - 自动重试原始请求并成功
   - 整个过程 < 1 秒

3. **控制台日志**
   - 清晰的刷新流程日志
   - 没有报错信息
   - Token 更新成功

### ❌ 失败的表现（需要修复）

1. **用户需要手动刷新页面**
   - 说明自动重试没有生效
   - 检查 `return api(originalRequest)` 是否执行

2. **页面直接跳转到登录页**
   - 说明刷新接口失败
   - 检查后端 Refresh Token 是否有效
   - 检查 Cookie 是否正确发送

3. **无限循环刷新**
   - 说明刷新锁没有生效
   - 检查 `isRefreshing` 逻辑

## 🐛 常见问题排查

### Q1: 刷新接口返回 401

**原因**：Refresh Token 过期或 Cookie 未发送

**解决**：
```javascript
// 检查 Cookie 是否存在
document.cookie  // 应该能看到 refreshToken

// 检查请求是否带上 Cookie
// Network -> refresh 请求 -> Headers -> Cookie
```

### Q2: 刷新成功但原始请求没有重试

**原因**：`return api(originalRequest)` 没有执行

**解决**：检查代码中是否有 `return` 关键字

### Q3: 并发请求时只有一个成功

**原因**：队列机制没有生效

**解决**：检查 `requestsQueue` 的 push 和执行逻辑

### Q4: 刷新后 Token 还是旧的

**原因**：没有更新内存中的 Token

**解决**：
```typescript
setAccessToken(newAccessToken)
useAuthStore.getState().setToken(newAccessToken)
```

## 📝 代码关键点说明

### 1. 响应拦截器中处理 40100

```typescript
// 在成功响应中也要检查业务错误码
if (res.code === 40100 || res.code === '40100' || res.errorCode === 40100) {
  return handleTokenExpired(response.config)
}
```

### 2. 自动重试的核心

```typescript
// 🔥 这一行是关键！返回重试的 Promise
return api(originalRequest)
```

### 3. 队列机制

```typescript
// 刷新中的请求进入队列
requestsQueue.push((newToken: string) => {
  originalRequest.headers.Authorization = formatAuthToken(newToken)
  resolve(api(originalRequest))  // 重试
})

// 刷新成功后批量执行
requestsQueue.forEach(callback => callback(newAccessToken))
```

## 🎉 测试通过标准

当你看到以下现象时，说明无感刷新已经成功：

1. ✅ Token 过期后，点击按钮数据正常显示
2. ✅ Network 面板显示 3 个请求（401 → refresh → 重试成功）
3. ✅ 控制台有完整的刷新日志
4. ✅ 用户完全无感知，体验流畅
5. ✅ 并发多个请求时，只刷新一次 Token

## 🚀 下一步优化（可选）

1. **Token 即将过期时主动刷新**
   - 解析 Token 的过期时间
   - 提前 1-2 分钟主动刷新
   - 避免用户操作时才发现过期

2. **刷新失败重试机制**
   - 网络抖动时自动重试刷新
   - 最多重试 3 次

3. **Token Rotation**
   - 每次刷新时更新 Refresh Token
   - 提高安全性

4. **单点登录**
   - 使用 Redis 存储 Refresh Token
   - 实现多设备互踢

---

**现在就去测试吧！** 🎯

如果遇到问题，查看控制台日志，根据上面的排查指南定位问题。
